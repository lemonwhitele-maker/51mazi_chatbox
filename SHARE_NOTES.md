# 51mazi_chatbox 源码包

这是当前工作版本的源码快照，版本号为 `0.8.5`。

## 启动方式

1. 安装 Node.js 和 npm。
2. 在项目根目录运行 `npm install`。
3. 运行 `npm run dev` 启动开发版。
4. 启动后先选择书库目录（例如 `BookList`），再在应用的 AI 设置中填写你自己的模型 API 配置；选择已有书库会加载其中保存的配置。

源码快照不包含本机 API key、私人书籍目录、依赖目录、构建输出和 Git 历史。源码仍包含功能样例与测试用合成文本。

## 人物图片与书库搬迁

- 在新版人物页面生成 **720×1280 竖版人物图**，预览并选择一张，点击“使用图片并保存人物”会同时保存头像和当前人物文字。每个人物关联一张头像，列表显示缩略图；发生版本冲突或保存失败时保留编辑内容与候选图片，处理后可重试。
- 已确认的人物图片保存在所属书本的 `character_images` 中，人物文档使用书内相对路径。已关联人物的关系图节点可同步头像，手动设置的节点头像保持独立；取消生成只清理本次临时图片，不自动删除以前保存的图片。
- 软件内的聊天模型、功能 API、DeepSeek、通义万相、Gemini、豆包以及上次选择的图片服务商，统一保存在 **`BookList/.51mazi/api-config.json`**，为应用共享配置，不需要每本书分别填写。
- 首次迁移先在书库 `.51mazi` 中保存仅含 API 配置的备份，再写入并校验新配置；全部成功后清除旧应用配置中的 API 项，主题、窗口等普通设置保持原位置。目标已有配置时保留目标值并补入缺项；文件损坏或写入失败时保留原配置。
- 换电脑时复制**完整 BookList（包含 `.51mazi`）**，在软件中重新选择该目录，即可加载书籍、已保存的人物图片和软件内 API 配置。切换到已有配置的书库使用其配置；目标没有配置则带入当前共享配置。
- 外部 Codex 的安装、登录和配置，以及尚未保存的编辑草稿，需另行处理。小说正文导出不包含 API 配置；直接分享整个书库时应排除 `.51mazi` 内的 API 配置及迁移备份。

## 本次旧功能清理

- 人物、设定和大纲使用 Markdown 知识文档；移除了旧人物完整档案与图库、紧凑树形大纲、旧大纲 AI 草稿工作台及其专用代码。
- 本次删除的是源码中的旧功能，不是书籍文件。用户选择的书籍主目录（例如 `BookList`）及其中已有的正文、资料、草稿和图片不受本次清理影响。
- 本版本不提供旧人物 JSON/HTML、旧分类设定 JSON、树形大纲和旧大纲 AI 草稿的自动迁移。旧文件即使仍保存在书籍目录中，也不会自动显示在新知识文档工作区。
- 人物头像选图、地址解析和关系图谱中的显示继续保留；人物图片生成已接入新版 Markdown 人物页面。此次只恢复单头像流程，不恢复旧人物档案表单或人物图库。

本分支基于小圣贤君的 51mazi 项目开发，保留原项目的版权和许可声明；详细许可见 `LICENSE`。
